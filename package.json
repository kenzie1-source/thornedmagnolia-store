{
  "name": "thornedmagnolia-store",
  "version": "1.0.0",
  "description": "Thorned Magnolia Collective E-commerce Store",
  "scripts": {
    "build": "cd frontend && npm run build && cp -r build/* ../public/",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": "22.x"
  }
}
